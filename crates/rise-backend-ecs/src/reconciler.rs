//! The in-process ECS reconcile loop.

// Implementation follows in the next commit; the pure modules it drives
// (sizing, tags, task_definition, service) land first so they can be reviewed
// on their own.
